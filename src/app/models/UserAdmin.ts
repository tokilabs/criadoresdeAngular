export interface UserAdmin {
  uid?: string;
  firstName?: string;
  id?: any;
  email: string;
  password: string;
  photoURL?: string;
  // isActive?: boolean;
  // registered?: any;
  // hide?: boolean;
}
